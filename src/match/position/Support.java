package match.position;

import match.Group;
import match.Hero;
import model.Player;

public class Support extends Hero {
    public Support(Player player, Group group){
        super(player, group);
    }

}
