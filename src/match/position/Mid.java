package match.position;

import match.Group;
import match.Hero;
import model.Player;

public class Mid extends Hero {
    public Mid(Player player, Group group){
        super(player, group);
    }

}
